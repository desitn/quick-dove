{
  "name": "quick-firmware-plus",
  "displayName": "%extension.displayName%",
  "description": "%extension.description%",
  "version": "0.2.6",
  "publisher": "destin-zhang",
  "icon": "images/icon.png",
  "galleryBanner": {
    "color": "#ffffff",
    "theme": "light"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Le-destin-jere/quick-firmware.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "dependencies": {
    "adm-zip": "^0.5.16",
    "iconv-lite": "^0.6.3",
    "ini": "^6.0.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "firmware-explorer",
          "title": "%extension.displayName%",
          "icon": "images/icon-quick.svg"
        }
      ]
    },
    "views": {
      "firmware-explorer": [
        {
          "id": "firmware-explorer",
          "name": "%view.firmwareList%",
          "icon": "$(list-tree)",
          "contextualTitle": "Quick Firmware +"
        },
        {
          "id": "firmware-devices",
          "name": "%view.deviceList%",
          "icon": "$(list-tree)",
          "contextualTitle": "Quick Firmware +"
        },
        {
          "id": "firmware-settings",
          "name": "%view.settings%",
          "icon": "$(gear)",
          "contextualTitle": "Quick Firmware +"
        }
      ]
    },
    "commands": [
      {
        "command": "firmwareDownloader.download",
        "title": "%command.download%",
        "icon": "$(arrow-circle-down)"
      },
      {
        "command": "firmwareDownloader.build",
        "title": "%command.build%",
        "icon": "$(coffee)"
      },
      {
        "command": "firmwareDownloader.refresh",
        "title": "%command.refresh%",
        "icon": "$(refresh)"
      },
      {
        "command": "firmwareDownloader.find",
        "title": "%command.find%",
        "icon": "$(search)"
      },
      {
        "command": "firmwareDownloader.clear",
        "title": "%command.clear%",
        "icon": "$(clear-all)"
      },
      {
        "command": "firmwareDownloader.devices_refresh",
        "title": "%command.devicesRefresh%",
        "icon": "$(refresh)"
      },
      {
        "command": "firmwareDownloader.addBuildCommand",
        "title": "%command.addBuildCommand%",
        "icon": "$(add)"
      },
      {
        "command": "firmwareDownloader.selectBuildCommand",
        "title": "%command.selectBuildCommand%",
        "icon": "$(check)"
      },
      {
        "command": "firmwareDownloader.configBuildCommand",
        "title": "%command.configBuildCommand%",
        "icon": "$(list-unordered)"
      },
      {
        "command": "firmwareDownloader.showWelcome",
        "title": "%command.showWelcome%",
        "icon": "$(home)"
      },
      {
        "command": "firmwareDownloader.showSetupWizard",
        "title": "%command.showSetupWizard%",
        "icon": "$(wand)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "firmwareDownloader.refresh",
          "when": "view == firmware-explorer",
          "group": "navigation"
        },
        {
          "command": "firmwareDownloader.find",
          "when": "view == firmware-explorer",
          "group": "navigation"
        },
        {
          "command": "firmwareDownloader.clear",
          "when": "view == firmware-explorer",
          "group": "navigation"
        },
        {
          "command": "firmwareDownloader.devices_refresh",
          "when": "view == firmware-devices",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "firmwareDownloader.download",
          "when": "view == firmware-explorer && viewItem == firmware-file"
        }
      ]
    },
    "configuration": {
      "title": "Quick Firmware +",
      "properties": {
        "quickFirmwarePlus.firmwarePath": {
          "type": "string",
          "default": "",
          "description": "%config.firmwarePath%"
        },
        "quickFirmwarePlus.buildCommands": {
          "type": "array",
          "default": [],
          "description": "%config.buildCommands%",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "命令名称"
              },
              "command": {
                "type": "string",
                "description": "构建命令"
              }
            }
          }
        },
        "quickFirmwarePlus.lastBuildCommand": {
          "type": "string",
          "default": "",
          "description": "%config.lastBuildCommand%"
        },
        "quickFirmwarePlus.buildGitBashPath": {
          "type": "string",
          "default": "",
          "description": "%config.buildGitBashPath%"
        },
        "quickFirmwarePlus.language": {
          "type": "string",
          "default": "auto",
          "enum": ["auto", "en", "zh-cn"],
          "enumDescriptions": [
            "%config.language.auto%",
            "%config.language.en%",
            "%config.language.zh-cn%"
          ],
          "description": "%config.language%"
        }
      }
    }
  }
}
